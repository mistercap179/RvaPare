<Window x:Class="WpfApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="410.265">
    <Grid Margin="0,0,2.2,-0.6">
        <TabControl HorizontalAlignment="Left" Margin="7,7,0,15.6" Width="391">
            <TabItem Header="Tab 1">
                <local:StudentWindow DataContext="{Binding StudentViewModel}" />
            </TabItem>
            <TabItem Header="Tab 2">
                <local:PredmetWindow DataContext="{Binding PredmetViewModel}" />
            </TabItem>
        </TabControl>

    </Grid>
</Window>
